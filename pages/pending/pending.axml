<view>
  <view>
    <tabs items="{{items}}" current="{{current}}" onChange="handleChangeTab">
      <view slot="title" slot-scope="item">
        <badge
          type="number"
          text="{{todoCount[item.value.value]}}"
          position="top-right"
        >
          {{item.value.title}}
        </badge>
      </view>
      <view class="content" slot-scope="content" a:for="{{ pendingList }}" a:key="*this">
        <pending-card  title="{{item.id}}" status="{{item.status}}" grade="{{item.grade}}" reportTime="{{item.reportTime}}" isExceed="{{item.isExceed}}" detail="{{item.detail}}" url="{{`/pages/pending/handleForm/formDetail/formDetail?id=${item.id}`}}">
      </view>
    </tabs>
  </view>
</view>